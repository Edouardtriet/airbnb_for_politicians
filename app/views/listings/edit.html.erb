<div class="container mt-5">
  <h1>Edit listing</h1>

  <%= render 'form', listing: @listing %>

  <div class="mt-3">
    <%= link_to "Back to listing", listing_path(@listing), class: "btn btn-outline-primary" %>
  </div>
</div>
