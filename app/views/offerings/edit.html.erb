<div class="container mt-5">
  <h1>Edit Offering</h1>

  <%= render 'form', offering: @offering %>

  <div class="mt-3">
    <%= link_to "Back to Offering", offering_path(@offering), class: "btn btn-outline-primary" %>
  </div>
</div>
